<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:conv="clr-namespace:WPF.Patterns.Convertors;assembly=PatternLibrary"
					xmlns:local="clr-namespace:DictionaryModule.Presenters"
					>
	<DataTemplate DataType="{x:Type local:DictionaryPresenter}">
		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto" MinWidth="100"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<ComboBox Grid.Column="0" ItemsSource="{Binding Dictionary}" DisplayMemberPath="Title" SelectedItem="{Binding SelectedItem}" VerticalAlignment="Center" HorizontalContentAlignment="Stretch"/>
			<Button Grid.Column="1" Command="{Binding AddCommand}" VerticalAlignment="Center" Padding="5,2,5,2" Content="+" Visibility="{Binding IsReadonly, Converter={x:Static conv:True.ToCollapsed}}"/>
			<TextBlock Grid.Column="2" Foreground="Black" Text="№" VerticalAlignment="Center" Margin="5" Visibility="{Binding IsNumbered, Converter={x:Static conv:False.ToCollapsed}}"/>
			<TextBox Grid.Column="3" Width="50" Text="{Binding CatalogNumber, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Visibility="{Binding IsNumbered, Converter={x:Static conv:False.ToCollapsed}}"/>
		</Grid>
	</DataTemplate>
</ResourceDictionary>