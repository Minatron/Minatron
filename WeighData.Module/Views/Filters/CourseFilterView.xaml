<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Band.OLD"
                    xmlns:conv="clr-namespace:Band.WPF.Convertors;assembly=WPF.Patterns">
    <DataTemplate DataType="{x:Type local:CourseFilterPresenter}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Foreground="Black" Text="{Binding Name}" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" MinWidth="80" IsReadOnly="True" Text="в Москву"  SelectedIndex="{Binding Course, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" >
                <ComboBoxItem Content="в Москву"></ComboBoxItem>
                <ComboBoxItem Content="в Санкт-Петербург"></ComboBoxItem>
            </ComboBox>
        </Grid>
    </DataTemplate>
</ResourceDictionary>